
---

# Claude Codeへの実装指示案

````markdown
# llm-zap-dast 認証付きDAST対応の実装指示

現在の `llm-zap-dast` プラグインへ、ZAPの認証機能を利用した
認証後DASTを追加してください。

対象リポジトリ：

https://github.com/shinya-shibasaki/llm-zap-dast

## 1. 今回の目的

今回の目的は、安定した汎用認証スキャナーを完成させることではありません。

任意のWebアプリケーションを対象に、LLMがソースコード、画面、DOM、
HTTP通信などを確認し、OWASP ZAPで利用可能な認証機能を選択・設定して、
認証後のDASTをどこまで自律的に実行できるか検証することです。

SASTにおける次の構成に対応するものと考えてください。

- SemgrepでSASTを実行する
- Semgrepの結果を確認する
- 追加でLLM自身がコードを読んでSASTする

DASTでは次を目指します。

- LLMがZAPの認証設定を構成する
- 認証済みユーザーでZAP Spider / Ajax Spider / Passive Scan / Active Scanを実行する
- ZAPの結果を確認する
- 追加でLLM自身が、ソースコードとZAP履歴を使ってシナリオベース診断を行う

認証設定や認証後スキャンに失敗しても構いません。
失敗した場合は、成功したように扱わず、実行できなかった工程と理由を
成果物および最終レポートへ明記してください。

## 2. 基本方針

認証方式ごとの独自認証エンジンを作り込まないでください。

ZAPが持つ以下の機能を、LLMが選択して操作できるようにしてください。

- Context
- Authentication Method
- Session Management Method
- Authentication Verification Strategy
- User
- Forced User ModeまたはUser指定スキャン
- Traditional Spider
- Ajax Spider
- Passive Scan
- Active Scan
- Browser Based Authentication
- Form-based Authentication
- JSON-based Authentication
- HTTP Basic Authentication
- Script-based Authentication
- Auto-Detect Authentication

利用可能な方式は、実際に使用するZAPバージョンのAPIと公式仕様を
確認してください。

LLMは、対象リポジトリのソースコード、ログイン画面、DOM、HTTP履歴を確認し、
対象に適した認証方式を判断します。

特定のWebアプリケーション専用のログイン処理をハードコードしないでください。

## 3. 成功を保証しないbest-effort型

この認証対応はbest-effort型です。

以下のいずれかになった場合は、無理に続行しないでください。

- 認証方式を特定できない
- ZAPへ認証設定を登録できない
- 認証成功を確認できない
- 意図したユーザーとして動作しているか分からない
- 認証状態を維持できない
- 認証済みUserを指定したSpiderまたはActive Scanが実行できない
- MFA、SSO、OAuth、SAML、CAPTCHA等により自動化できない

その場合は、たとえば次のように記録してください。

- 認証設定：成功 / 失敗 / 未確定
- 認証確認：成功 / 失敗 / 未確定
- 認証後Spider：成功 / 失敗 / 未実施
- 認証後Passive Scan：成功 / 失敗 / 未実施
- 認証付きActive Scan：成功 / 失敗 / 未実施
- LLM追加診断：成功 / 一部成功 / 未実施
- 理由
- 診断できなかった範囲

認証成功が曖昧な場合、認証済みとして扱わないでください。

## 4. 既存フローへの追加

既存の工程0〜7は維持してください。

新しいSkillへ分割せず、既存の `/llm-zap-dast:dast` に統合してください。

### 工程0：安全確認

既存の確認に加えて、以下を確認してください。

- `authentication.enabled`
- 認証情報用環境変数の存在
- 認証設定に平文のユーザー名やパスワードが書かれていないこと
- 認証後Active Scanが有効か
- 認証後に到達し得る状態変更機能
- ログイン試行回数の上限

認証情報の値は出力しないでください。

### 工程1：ソースコード解析

既存の解析に加えて、次を抽出してください。

- ログインURLまたはログインAPI
- 認証方式の候補
- ユーザー名・パスワードのパラメータ名
- フォーム、JSON、Basic、トークン、Cookie等の形式
- セッションまたはトークンの保持方式
- ログイン成功を示す候補
- ログアウト状態を示す候補
- 認証後にのみ利用できるURLまたはAPI
- ログアウトURL
- パスワード変更、アカウント削除などの危険なURL

ソース由来の事実とLLMの推測を区別してください。

### 工程2：診断対象マップ

各エンドポイントについて、以下を追加してください。

- 認証要否
- 必要なロールまたは権限
- 未認証時に期待される応答
- 認証後に期待される応答
- 状態変更の有無
- Active Scan対象に含めてよいか
- 認証後に新たに除外すべきか

### 工程2.5：認証設定と認証確認

工程2と工程3の間に、認証処理を追加してください。

1. 対象に適したZAP認証方式をLLMが選択する
2. ZAP Contextへ認証方式を設定する
3. Session Management Methodを設定する
4. Authentication Verification Strategyを設定する
5. ZAP Userを作成する
6. 環境変数から資格情報を読み、Userへ設定する
7. 認証済みUserとしてテストリクエストを送る
8. 認証後にのみ到達できるURLまたはAPIへアクセスできるか確認する
9. 未認証時の応答と認証後の応答を比較する
10. 成功を確認できた場合だけ認証後工程へ進む

認証設定に失敗した場合は、未認証の診断を継続して構いません。
ただし、認証後診断は未実施として記録してください。

### 工程3：認証後探索

認証に成功した場合は、認証済みUserまたは認証済みブラウザとして以下を行ってください。

- Traditional Spider
- 必要に応じてAjax Spider
- Passive Scan
- URL、HTTP履歴、アラートの取得

認証済みで実行した探索と、未認証で実行した探索を区別してください。

次のような比較成果物を作成してください。

`reports/dast/<run-id>/authentication.md`

含める内容：

- 採用した認証方式
- 採用理由
- ZAP Context名
- ZAP User名
- 認証成功の証拠
- 認証失敗の証拠
- 未認証時に到達したURL数
- 認証後に到達したURL数
- 認証後に新しく到達したURL
- セッション維持状況
- 再認証の有無
- 制約と失敗事項

### 工程4：ブラウザ探索

ZAPの認証機能でブラウザ操作が必要な場合は、
Browser Based AuthenticationまたはZAP経由のPlaywrightを使用してください。

Playwrightを利用する場合も、通信をZAP Proxyへ通してください。

ただし、Playwrightでログインできたことと、
ZAP Userとして認証付きSpider / Active Scanができることは別です。

それぞれを別々に判定・記録してください。

### 工程5：認証付きActive Scan

認証成功が確認できた場合、認証済みUserを指定してActive Scanを実行してください。

単に認証後URLをContextに入れるだけではなく、
ZAPのUserまたは認証済みコンテキストを明示的に使ってください。

実行前に既存のActive Scanゲートを通してください。

利用者へ次を提示し、明示確認を取ってください。

- 対象URL
- 使用するContext
- 使用するUser
- 認証方式
- 認証後に到達したURL
- 除外URL
- 状態変更の可能性があるURL
- 使用するScan Policy
- 想定リクエスト数または負荷
- 認証済み状態であるため未認証スキャンより影響範囲が広いこと

認証後に発見された危険なURLを `exclude.paths` に追加候補として提示し、
確認後にActive Scan対象から除外してください。

Active Scan中に認証状態が失効した可能性がある場合は、
結果を認証済みスキャン成功と断定しないでください。

### 工程6：LLMによる認証後の追加診断

ZAPの認証付き自動診断結果に加えて、LLM自身が次を行ってください。

- ソースコードを読む
- 認証後のZAP HTTP履歴を読む
- 認証・認可・セッションに関する仮説を立てる
- ZAP ProxyまたはZAP API経由で対象固有のリクエストを組み立てる
- 応答を比較する
- 必要に応じてペイロードを調整して再試行する
- 確認済み、候補、否定、未確認に分類する

対象例：

- 認証回避
- 未認証アクセス
- 垂直権限昇格
- 水平権限昇格
- IDOR
- セッション固定
- Cookie属性
- JWTまたはトークンの検証不備
- ログアウト後セッション無効化
- CSRF
- Mass Assignment
- 隠しパラメータ
- HTTPメソッド変更
- 認証後のSQLi / XSS / コマンドインジェクション
- 業務ロジック不備

単一ユーザーしか設定されていない場合は、複数ユーザーを必要とするIDORを
無理に確定しないでください。

実施できる範囲まで試し、実施できなかった理由を記録してください。

### 工程7：レポート

最終レポートに次を追加してください。

- 認証方式
- 認証設定結果
- 認証確認結果
- 認証後探索結果
- 認証付きActive Scan結果
- 認証状態を維持できたか
- 未認証と認証後のカバレッジ差
- LLM追加診断結果
- 実施できなかった認証工程
- 認証後診断で未確認となった範囲

## 5. 設定スキーマ

`dast.yaml` の `authentication` を拡張してください。

基本形：

```yaml
authentication:
  enabled: false

  # auto | browser | form | json | basic | script
  method: auto

  login_url: /login

  username_env: DAST_USERNAME
  password_env: DAST_PASSWORD

  max_attempts: 3

  verification:
    method: auto

    # 必要な場合だけ利用者が指定する。
    logged_in_indicator:
    logged_out_indicator:
    verification_url:

  session_management:
    method: auto

  active_scan: true
````

要件：

* 既定は `enabled: false`
* 平文の `username` / `password` キーは禁止
* 認証情報は環境変数からのみ取得
* `method: auto` の場合はLLMが対象を解析して方式を選択
* 未指定項目は推測してよいが、採用した値と理由を記録
* 推測できない場合は認証工程を失敗として終了
* 既存の未認証設定との後方互換を維持

`scan.active_scan` は既存のActive Scan全体のゲートとして維持し、
`authentication.active_scan` は認証付きActive Scanを許可する追加ゲートとしてください。

両方がtrueで、工程5の利用者確認を通った場合だけ認証付きActive Scanを実行してください。

## 6. スクリプト

LLMがZAP APIを毎回場当たり的に呼ぶだけでなく、機械的な処理は薄いラッパへ分離してください。

ただし、対象固有の認証手順をハードコードしないでください。

追加候補：

```text
scripts/zap_auth.py
```

想定する操作：

```text
detect-capabilities
create-context
configure-authentication
configure-session-management
configure-verification
create-user
set-credentials
test-authentication
spider-as-user
ajax-spider-as-user
active-scan-as-user
auth-status
clear-authentication
```

スクリプトは判断を行わず、引数で指定された設定をZAP APIへ反映するだけにしてください。

LLMはソースと画面を解析し、どの引数を渡すか判断します。

ZAPバージョンによって利用できるAPIが異なる場合は、
`detect-capabilities` で利用可能な方式を取得し、未対応方式を明示してください。

## 7. redaction

認証対応により、資格情報とセッショントークンが成果物へ入りやすくなります。

次を必ずマスクしてください。

* ユーザー名
* パスワード
* Cookie
* Set-Cookie
* Authorization
* Basic認証値
* JWT
* Access Token
* Refresh Token
* CSRF Token
* ZAP Userに保存された資格情報
* ログインPOST本文

資格情報の値を以下へ出さないでください。

* run.log
* JSON出力
* 標準出力
* 標準エラー
* report.md
* authentication.md
* screenshots
* Playwright trace

可能な範囲でrun終了時に、一時的なZAP User、Context、
ブラウザプロファイル、認証用一時ファイルを削除してください。

## 8. 安全性

高い安定性を保証することは今回の目的ではありませんが、
以下の既存安全ルールは維持してください。

* allowed_hosts外へ送信しない
* ATTACKモードを使わない
* 本番環境は既定で拒否
* Active Scan前に明示確認を取る
* exclude.pathsを適用する
* DoS相当の検証を行わない
* 不可逆操作を自動実行しない
* 認証情報を成果物へ保存しない
* ログイン試行は `max_attempts` 以下
* 認証成功が不明なら認証済みと扱わない

認証後Active Scanは、通常のActive Scanより影響範囲が広いことを
確認画面とレポートへ明記してください。

## 9. テスト

実物の対象アプリを必須としない単体テストに加えて、
可能ならローカル簡易アプリによる統合テストを追加してください。

最低限のテスト：

* `authentication.enabled: false` の後方互換
* 平文資格情報設定を拒否
* 環境変数不足を検出
* `max_attempts` の検証
* 未対応認証方式を検出
* ZAP User作成APIの組み立て
* 認証確認失敗時にActive Scanを呼ばない
* 認証成功時にUser指定Active Scanを呼ぶ
* `scan.active_scan` または `authentication.active_scan` がfalseなら
  認証付きActive Scanを呼ばない
* 認証情報がJSON・ログ・レポートに残らない
* 認証失敗がレポートに記録される
* 認証後の新規URLを区別できる

## 10. READMEとreference更新

次を更新してください。

* `README.md`
* `skills/dast/SKILL.md`
* `references/zap-integration.md`
* `references/safety-policy.md`
* `references/source-analysis.md`
* `references/scenario-testing.md`
* `references/redaction.md`
* `references/report-format.md`
* `references/config-init.md`
* `templates/dast-config.example.yaml`
* `templates/report.example.md`
* `examples/dast.yaml`
* `validate_config.py`
* 関連テスト

READMEには、この認証対応がbest-effortであり、
任意のWebアプリでの認証成功を保証しないことを明記してください。

## 11. 実装前に行うこと

すぐに変更を始めないでください。

最初に次を行ってください。

1. 現在の認証関連実装を調査する
2. 使用するZAPバージョンで利用可能な認証APIを公式資料から確認する
3. 現在の工程0〜7への組み込み案を提示する
4. 変更予定ファイルを列挙する
5. `zap_auth.py` のコマンド設計を提示する
6. `dast.yaml` の更新案を提示する
7. 認証成功・失敗・未確定の状態モデルを提示する
8. 既存機能への影響を整理する
9. 実装を複数段階に分けた計画を提示する

計画を提示した後、実装を開始してください。

## 12. 完了条件

以下を満たしたら完了です。

1. 認証無効時に既存の未認証DASTが動作する
2. LLMが対象から認証方式の候補を導出できる
3. ZAPへContext、認証方式、Session Management、Verification、Userを設定できる
4. 認証成功を確認できる
5. 認証失敗を成功として扱わない
6. 認証済みUserでSpiderまたはAjax Spiderを実行できる
7. 認証済みUserでPassive Scan結果を取得できる
8. 明示確認後に認証付きActive Scanを実行できる
9. Active Scan結果をLLMが確認できる
10. LLMがZAP履歴とソースを使って追加診断できる
11. 認証に失敗した場合も理由をレポートできる
12. 資格情報・Cookie・JWTが成果物へ平文で残らない
13. 認証無効時との後方互換が維持される
14. テストが成功する
15. READMEだけで設定・実行・制約を理解できる

````

---

## この指示で重要な点

現在のZAP連携リファレンスは、Context、Spider、Active ScanなどのAPI一覧はありますが、認証、Session Management、User、User指定スキャンのAPIはまだ含まれていません。

したがって、単に`SKILL.md`へ「ログインしてください」と追加するだけでなく、次の2層で実装させるのがよいです。

```text
LLM
└─ ソース・画面・HTTP通信を見て設定値を判断

zap_auth.py
└─ LLMが決めた設定値をZAP APIへ機械的に反映
````

SASTに置き換えると、

```text
LLM
└─ Semgrepをいつ、どう使うか判断

Semgrep
└─ 決められたルールで機械的にスキャン
```

に近い構造です。

## 実装を段階分けする場合

Claude Codeには一度に全機能を作らせてもよいですが、実装確認は次の順番が安全です。

1. **認証設定と認証確認**

   * Context
   * Authentication
   * Verification
   * User
   * テストリクエスト

2. **認証後探索**

   * User指定Spider
   * Ajax Spider
   * Passive Scan
   * 未認証との差分

3. **認証付きActive Scan**

   * 追加ゲート
   * User指定Active Scan
   * セッション維持確認

4. **LLM追加診断**

   * 認証後履歴
   * ソース解析
   * 対象固有プローブ
   * 結果判定

5. **redaction・レポート・テスト**

この順序なら、途中でZAPの認証APIに制約が見つかっても、その時点までの成果を残せます。

なお、現在の設定例では`scan.active_scan: true`が既定になっています。
認証付きActive Scanを追加する場合は、本文案のように、

```yaml
scan:
  active_scan: true

authentication:
  active_scan: false
```

と別ゲートにして、**認証後Active Scanは新規設定では既定OFF**にする方が分かりやすいです。
