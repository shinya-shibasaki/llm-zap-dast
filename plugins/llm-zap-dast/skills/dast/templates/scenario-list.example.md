# シナリオベース診断 — <run-id>

> 破壊的なペイロード不可。可用性を損なう（DoS相当）検証不可。
> allowed_hosts のホストのみ。exclude.paths を守る。証拠はマスクする。

---

## SC-001 — /api/users/:id の IDOR

- 対象機能：ユーザープロフィール取得
- 想定脆弱性：IDOR（水平アクセス）
- 根拠 `[S]`：ハンドラが path の `:id` を読む；認可チェックの位置がソース上で不明。
- 前提条件：低権限アカウント A と B（テストデータのみ）。
- 手順：
  1. A として `/api/users/<A-id>` を取得（ベースライン）。
  2. A として `/api/users/<B-id>` を取得。
- 期待される安全な挙動：Bのレコードには 403/404。
- 脆弱な場合：A が B のデータを取得（200 で B のフィールド）。
- 実行可否：可（非破壊・読み取りのみ）。
- 結果：<未実行 / 安全 / 脆弱>
- 証拠：<マスク済みリクエスト/レスポンスへの参照>
- 追加確認：他ロール間でも確認；他の `/api/*/:id` ルートを確認。

---

## SC-002 — <タイトル>

- 対象機能：
- 想定脆弱性：
- 根拠：
- 前提条件：
- 手順：
- 期待される安全な挙動：
- 脆弱な場合：
- 実行可否：
- 結果：
- 証拠：
- 追加確認：
