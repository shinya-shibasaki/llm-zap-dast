# DAST レポート — <run-id>

_LLM支援型グレーボックスDAST（ソース解析＋OWASP ZAP＋ブラウザ）。完全な手動ペネトレーション
テストの代替ではありません。すべての検出結果は人間の確認を要します。_

## 1. 診断概要
<何を、どの粒度で診断したか>

## 2. 対象情報
- ベースURL / ホスト：<...>
- スコープ（allowed_hosts）：<...>
- 除外パス：<...>

## 3. 実行日時
- 開始 / 終了：<...>  ・ run-id：<...>

## 4. 使用したツール
- OWASP ZAP <バージョン>、モード：Protected（起動：<手動 / スキルが自動起動>）
- Playwright：<使用 / 利用不可>
- スクリプト：validate_config.py、check_environment.py、redact.py、zap_control.py、
  zap_auth.py（認証付き run 時）

## 5. 実行した診断工程（スキップ＋理由を含む）
| 工程 | 状態 | スキップ理由 |
|------|------|--------------|
| 0 安全確認 | 実行 | |
| 1 ソース解析 | 実行 | |
| 2 診断対象マップ | 実行 | |
| 2.5 認証 | 実行 / 未実施 | <`authentication.enabled: false` のため未実施 ／ 認証できず run 停止> |
| 3 ZAP探索 | 実行 / スキップ | <例：ZAP不達> |
| 4 Playwright | 実行 / スキップ | <例：Playwright未導入> |
| 5 Active Scan | スキップ | <`active_scan: false` / ゲート条件が未充足・曖昧> |
| 6 シナリオ | 実行 | |
| 7 レポート | 実行 | |

**SAST 連携（`sast.enabled: true` のとき）**：使用した SAST run = `<run-id>` / `reports/sast/<run-id>`。
SAST 側の縮退内容はここへ転記しない（原本の §0 に第一級で書かれている。run-id があれば辿れる）。

## 6. 探索範囲（カバレッジ）
<実際に到達したURL/エンドポイント>

## 7. 未到達範囲
<coverage-analysis.md より、分類付き>

## 8. 検出結果一覧

> DAST の検出には **`D-` 接頭辞**を使う。`sast.enabled: true` の run では SAST の指摘 ID
> （`F-01` / `Q-01`）が同じ文書に並ぶため、`F-1` のような採番は取り違えの元になる。

| ID | タイトル | 事実の出所 | 確度 | 深刻度 |
|----|----------|-----------|------|--------|
| D-01 | <...> | ツール / HTTP / **この run のソース** / **伝聞 `[H]`（SAST 由来。指摘IDを併記）** / 推測 | 再現できた / 再現できなかった / 誤検知の可能性 / 要人間 | <...> |

### 8b. SAST 指摘 × DAST 検証（`sast.enabled: true` のときのみ）

> 分母は診断対象マップの「SAST 指摘の根拠」節と「横断・非エンドポイント指摘」節の**全件**。
> **無言の打ち切り禁止はこの表にも掛かる**（空欄で濁さない）。
> **DAST が再現できなかったことを SAST 指摘の格下げ根拠にしない** — 「再現できなかった」と
> 「誤検知の可能性」は別の値（`references/report-format.md`）。

| SAST ID | 指摘（1行） | SAST 深刻度 | 突合先 | DAST の判定 | 根拠 / 理由 |
|---|---|---|---|---|---|
| F-01 | ログイン欄の SQL インジェクション | Critical | マップ #1 | **確認** | D-01。認証を回避できた（証拠 §10） |
| Q-01 | 署名検証関数の呼び出しが正しいか | 要確認 | マップ #2 ほか | **否定** | 無効トークンで 401。静的に解けなかった点を動的に決着 |
| F-45 | 到達経路が無いとされたコード実行面 | Low | マップ #12 | **否定** | SAST の静的な否定を DAST で追認した |
| F-04 | 圧縮ファイル展開でのパストラバーサル | Critical | マップ #5 | **要人間** | (a)試行 … (b)8Aだが `destructive:false` (c)次の一手 … |
| F-09 | パスワードハッシュが弱い | Low | §3（非エンドポイント） | **該当なし** | HTTP からは観測できない。オフライン解析は範囲外 |
| Q-06 | 依存の既知脆弱性 | 要確認 | §3（非エンドポイント） | **該当なし** | SCA は本プラグインの境界外（外部ツールを足さない） |
| F-32 | オブジェクト注入 | Medium | — | **未突合** | SAST 側の URL が実装と食い違い、安全に貼れる行が特定できなかった。曖昧一致で別のエンドポイントへ貼らない |

**集計**：SAST 指摘 N 件 → 確認 a / 否定 b / 要人間 c / 未実施 d / 該当なし e / 未突合 f。

## 9. 再現確認結果
<再現したもの、しなかったもの>

## 10. 証拠（マスク済み）
<マスク済み成果物への参照；平文の秘匿情報は含めない>

## 11. リスク説明
<確認済み/可能性の高い各検出の影響>

## 12. 修正案
<具体的な修正>

## 13. 未確認事項

**「要人間」の各項目**には監査のため次を必ず書く（`references/report-format.md`・
`references/scenario-testing.md` の決定木）。**能力欠如は「要人間」ではなく「未実施（理由＋工程0の
チェック結果の引用）」**に分類する。

| 項目 | (a) 試行した検証 | (b) ブロッカー種別 | (c) 次の一手 |
|------|------------------|--------------------|--------------|
| <例：割引ロジックの妥当性> | curl で境界値を送信し応答を記録 | 自動化不能な人間判断 | 仕様と突合 |
| <例：課金完了時の副作用> | 送信前に宛先設定を確認（外部の決済API） | 8C要 | 手動でサンドボックス鍵に差し替えて実施 |

(b) は **8C要／8B要／8Aだが `destructive:false`／自動化不能な人間判断** の4種のみ。

**未実施（要人間ではない）**：<能力欠如・運用上の回避・除外パス・時間切れの項目と理由>

## 14. 診断上の制約
- グレーボックス・LLM支援；認証は「できなければ停止」（`authentication.enabled` 時、任意アプリでの
  認証成功は保証しない）；キーなしZAPはローカル限定；Active Scanはゲート付き。
- **破壊的検証**：`scan.destructive`＝<有効 / 無効>。有効時は対象アプリ内部の不可逆な状態変更まで
  実施（使い捨てローカル対象前提）。外部への副作用（外部メール/課金/外部SSRF等）は実施していない。
- **可用性への影響**：`scan.availability_impact`＝<有効 / 無効>。無効時はDoS相当の検証なし。
- **認証アカウント構成**（該当時）：<単一 / 同一ロール2 / 異ロール / 3>。実施できた認可クラスと
  できなかったクラスは 5〜7・`authentication.md` を参照。

## 15. 免責事項
許可されたホストのみを診断しました。結果には誤検知/検知漏れが含まれ得るため、対応前に人間の確認を
要します。本診断は**自分が所有する使い捨てのローカル脆弱アプリ**を対象とし、`scan.destructive`
有効時は対象内部を不可逆に変更する検証を含みます（何を変更したかは本文に記録）。サンドボックスの外
（外部サービス・実在インフラ）への副作用は行っていません。
