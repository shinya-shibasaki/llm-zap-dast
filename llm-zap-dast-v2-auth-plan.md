# llm-zap-dast v2：認証対応の方針メモ

このメモは、v2で認証（ログイン）を扱えるようにするための方針を、これまでの検討結果として
まとめ直したものです。実装指示書（`llm-zap-dast-implementation-instructions.md`）を上位の
根拠とし、それを**認証について具体化**したものです。指示書と衝突する場合は指示書が優先します。

---

## 0. 一番大事な方針（North Star）

**「シンプルな土台を作り、高度な診断はLLMが都度判断する」。**

- 実装するのは、LLMが認証付きでDASTを回せるための**最小限の土台**だけ。
- IDOR・権限昇格・セッション/JWT改ざんといった高度な診断は、**個別機能として作り込まない**。
  これらは工程6で**LLMがその都度、仮説を立て・組み立て・実行する振る舞い**として扱う。
- これはこのプラグインの既存思想（LLMがフローを駆動し、スクリプトは薄く、詳細は
  `references/` に置く）の延長であり、新しい複雑さを持ち込むものではない。未認証の工程6が
  既にこの形になっている（それを「ログイン済みでもできる」ようにするだけ）。

この方針により、「シンプルにしたい」と「高度な診断も落としたくない」は両立する。高度な診断を
"機能として先送り"するのではなく、そもそも"機能にしない"＝LLMが都度やる、という切り分け。

---

## 1. 指示書との関係（確認）

指示書 8章（`dast.yaml` 例）に、v2認証の方針が既に書かれている。本メモはこれに準拠する。

- **v2の主機構 = ZAP Browser Based Authentication（要 ZAP 2.16.1+）。**
- **Playwrightログインは fallback**（ZAPが扱えないログインフロー向け）。
- 選定理由（指示書に明記）：ZAPネイティブ認証は**自動再認証**を持つが、Playwrightの手動認証は
  再認証を持たないため長時間スキャンで不利。
- 設定スキーマは `method: browser | playwright` と**単一ユーザー**
  （`username_env` / `password_env` 各1つ）。

補足：BBAは**ZAP自身が実ブラウザでログインを代行**する方式。SPA/CSRF/JSログインに対応しつつ、
ログイン手順をZAPが所有するので**失効時に自動再認証できる**。こちら側で作るのは「BBAを設定する
薄い部分」だけで済み、シンプル方針と相性が良い。

---

## 2. 作るもの（土台・最小限）

LLMに任せる前提として、これだけは土台側に固定で用意する。いずれも小さく保つ。

### 2.1 認証の入口（ZAP BBA 設定）
- ZAP Browser Based Authentication を設定する薄いZAP-APIラッパ（既存スクリプトと同様、
  ステートレス・機械的処理に徹する。「認証エンジン」化しない）。
- BBAが扱えないと判断された場合の **Playwrightログイン fallback**（再認証なしを承知で使用）。
- セッション確立の置き場所：工程2（target-mapで認証要と判明）と工程3（Spider）の**間**、または
  工程3の前準備（ZAP Context作成と同じ枠）に畳み込む。**フロー構造は崩さない**（追加であって
  再構築ではない）。

### 2.2 設定スキーマ（`authentication` ブロック）
- 指示書の単一ユーザースキーマを踏襲：`enabled` / `method`（browser | playwright）/ `login_url`
  / `username_env` / `password_env`。
- `validate_config.py` を更新：方式別の整合チェック、**env変数名のみ**（秘匿値の直書きは
  構造的に拒否＝`password:` のような値持ちキーを禁止し、`*_env` は実在する環境変数を指すこと）、
  `enabled: false` 既定の後方互換。

### 2.3 マスク（redaction）の拡張
認証を入れると毎runで資格情報POSTが発生するため、現状のマスク（ZAP JSON限定）を広げる。
- `redact.py` にログインのフィールド名を動的に渡す＋`*password*`/`*passwd*` 部分一致＋
  username値もマスク。
- **これまでノーガードだった漏れ口**を塞ぐ：`run.log`、認証スクリプトの標準出力、
  ZAPセッション/Usersストア（平文パスワード保持）、Playwrightのスクショ/トレース。
- 検証テスト：`reports/dast/<run-id>/` ツリー全体（`run.log` 含む）を資格情報値でgrepして
  ヒットしたら失敗、を追加。

### 2.4 固定の安全ルール（§4に詳細）
LLMの裁量にしない少数の"常にこうする"ルール。機構ではなく数行のルールとして
`references/safety-policy.md` に固定し、可能な範囲でスクリプトにも実装する。

---

## 3. LLMに都度任せるもの（新規コードなし）

認証後の**診断そのもの**は、工程6の振る舞いとしてLLMが都度判断する。個別機能を作らない。

- **どの認証方式が該当するか**の判定（form/token/basic/SPA）をソースから読み取る。
- ログインのセレクタ、「認証成功」を示す指標文字列、logged-in/out判定 → **対象のソース/DOMから
  LLMが導く**。
- **多ロールIDOR**：LLMが工程6で「アカウントAでログイン→叩く→アカウントBでログイン→他人の
  リソースが見えるか比較」を**都度実行**。ZAPの重い多ユーザー機構は使わない（＝シンプル）。
- **垂直権限昇格**：低権限セッションで管理者ルートを叩き、拒否されるか確認。
- **セッション/JWT改ざん・認証回避**：改ざんペイロード（`alg:none`・期限切れ・別人のトークン）を
  組み立てて確認。
- これらの payload・対象・観察方法は、**今の未認証プローブと同じ裁量**でLLMが決める。

---

## 4. 固定の安全ルール（LLMの裁量にしない）

以下は「LLMが都度判断」に乗せず、**固定**とする。理由：ツール（ZAP）の事実、または安全の
不変条件であり、間違えると**静かに壊れる**（誤った身元で走査して偽の結果を作る／アカウント
ロックアウト／秘匿情報漏洩）ため。

1. **資格情報を成果物に残さない**（§2.3の漏れ口封じ＋fail-closed）。
2. **ログイン試行バジェット＋バックオフ**。セッション改ざん系はわざとログアウトを誘発するため、
   自動再認証と組み合わさるとログイン連打→ロックアウト（＝可用性影響、既存の禁止事項）。
   ロックアウトを誘発しない上限を固定し、ログアウト誘発プローブ中は自動再認証を切る。
3. **身元の検証と、曖昧なら停止**。「意図したロールで認証できたか」を確認し、曖昧なら認証部分を
   **停止**（fail-softで流さない）。※ルールは固定、"何を見れば成功と分かるか"の中身はLLMが導く。
4. **認証後の Active Scan は exclude.paths を作り直す**。未認証の除外リストは、認証後に現れる
   新しい状態変更エンドポイント（アカウント設定・削除等）を想定していないため。認証後の
   target-mapから再導出する。
5. **認証後の Active Scan はロール別に工程5の確認を取る**（認証済みのblast radiusを明示）。
6. 既存の安全不変条件（許可ホストのみ・スコープはZAP Context・ATTACKモード禁止・本番既定拒否・
   非破壊/DoS禁止）はそのまま維持。

### 線引きのルール（迷ったとき）
- **LLMが都度決める**：答えが*対象のソース/挙動*で変わり、かつ間違えれば*その場で派手に分かって
  直せる*もの（方式選択・セレクタ・指標・payload・どのアカウントを使うか）。
- **土台に固定**：答えが*ZAPというツールの性質*、または*安全の不変条件*で、対象によらず一定、
  かつ間違えると*静かに壊れる*もの（機構の組み合わせ制約・上記の安全ルール）。
- 二つの問い：「対象のソースで答えが変わる？」Yes→LLM／No→固定。「間違えたら派手に分かる？
  静かに壊れる？」派手→LLM／静か→固定。

---

## 5. 作る前に一度だけ確かめること（スパイク）

「LLMに都度任せる」を安全にするため、**ZAPの機構が実際にどう動くかを一度実証して**、その結果を
`references/` に"使えるメニューと制約"として書いておく（LLMは*選ぶ*、*発明しない*）。これは新しい
手順ではなく、指示書11章が既に求めている「v1でProtectedモードの実効を一度確認する」の類推。

確認項目：
1. ZAP 2.16.1+ で BBA が実ログインを駆動できるか（バージョン前提の確認。現行 `zap-integration.md`
   は 2.14+ 記載なので v2 で更新）。
2. BBA の**自動再認証**が実際に効くか。
3. BBA で扱えないフローの**判定基準**と、Playwright fallback の**発火条件**。
4. Playwright を ZAP プロキシ経由で使う経路と、ZAP側認証が競合しないか（fallback使用時のみ）。

スパイクを飛ばして未実証の機構にLLMが即興で当たると、例：SPA/JWT対象で「認証済み」と信じたまま
匿名で走査し、レポートに「走査済み」と書いてしまう（静かな偽陰性）——を招く。だから*機構の実証*
だけは前倒しする。

---

## 6. スコープ

### v2で土台として作る（最小限）
- ZAP BBA 認証の入口（＋Playwright fallback の器）
- `authentication` スキーマと `validate_config.py` の更新（単一ユーザー、env名のみ）
- redaction の拡張（§2.3）と grep 検証テスト
- 固定の安全ルール（§4）を `references/safety-policy.md` に明記
- 認証工程をフローに挿入（工程2→3の間／工程3前準備）
- スパイク結果を `references/zap-integration.md` に「認証メニューと制約」として記載
- 認証付き Spider / Passive まで（＝認証済みカバレッジ）

### v2ではLLMの振る舞いとして扱う（新規コードなし）
- 認証後の高度な診断すべて（IDOR・権限昇格・セッション/JWT改ざん・認証回避 …）を工程6で都度実行

### v2で既定OFF（安全ゲートを維持）
- 認証付き Active Scan は、§4-4/§4-5（exclude再導出＋ロール別確認）を満たしたときのみ。
  既定は未認証Active Scanと同じくゲート越し。

### 明示的に対象外
- 多ロールの**ZAPネイティブ自動多ユーザー機構**（重いので作らない。多ロールIDORは工程6で
  LLMがアカウントを切り替えて実現）
- MFA / SSO / OAuth / SAML（v2では扱わない）
- 認証エンジン化・独自の再認証オーケストレーション（BBA/LLMに委ねる）

---

## 7. フローへの組み込み（崩さない）

既存の工程0〜7に認証のスロットは既にある（工程0で要否記録、工程1でログインフロー抽出、
工程2でエントリごとの認証要否、工程4でログイン操作、工程6で認証前提プローブ）。v2は**空の
スロットを埋める**だけ。新規に足す離散アクションは「セッション確立」1つで、置き場所は工程2→3間。

注意：セッションの**再認証**は"工程"ではなく、BBAが担う下回りのインフラとして扱う（工程として
モデル化しない）。

---

## 8. 決めること / 未解決

- スパイク（§5）の実施タイミング（実装着手前に1回）。
- Playwright fallback を v2 初回に含めるか、BBA だけで出して fallback は次スライスか。
- 認証付き Active Scan を v2 に含めるか（既定OFF・ゲート越しなら含めても安全側だが、
  シンプル優先なら次スライスでも可）。

---

## 付録：これまでの検討で確認した事実

- v1 は認証を実行しない（`validate_config.py` は整合チェックのみ、ログイン実装なし）。
- 指示書が BBA 主・Playwright 従を選定済み（8章）。この選定は「ブラウザログインは自動再認証と
  噛み合わない」という懸念を機構選定の時点で回避している（BBAはZAPが手順を持つため再認証可）。
- 現状の redaction は ZAP JSON にしか効かず、`run.log`・ZAPセッション・スクショは対象外。
  認証はこれらを秘匿情報の漏れ口にするため、§2.3で塞ぐ必要がある。
