# llm-zap-dast 認証付きDAST対応 実装指示（改訂版・実験エディション）

この文書は `認証の実装方法案.md`（ChatGPTと共作した初版）を、これまでの敵対的レビューと
方針確認を反映して刈り込んだ改訂版です。初版の良い骨格（工程0〜7統合・best-effort・状態モデル・
薄い `zap_auth.py`・redaction漏れ口リスト・二重ゲート）は維持し、**安全の急所2点を安価に固定**、
**過剰な完成度要求は best-effort に緩和**しています。初版と本改訂版が食い違う場合は本改訂版を優先。

---

## 0. 前提（この実装の位置づけ）

- **目的は「完成した汎用認証スキャナー」ではなく、「LLMにDASTを任せたらどこまでできるか」の検証。**
  完成度・堅牢性が多少低いのは許容する。失敗は「欠陥」ではなく「実験の観測結果」として記録する。
- **対象は原則、自分が所有する使い捨てのローカル脆弱アプリ**（例：OWASP Juice Shop 等）。
  **本番・現実の稼働アプリには向けない。** 既存のローカル限定レール（`allowed_hosts`・キーなし＝
  ローカル限定・`allow_production: false` 既定・ATTACKモード拒否）は維持し、これが「うっかり本番」を
  構造的に防ぐ。
- **認証付き Active Scan は ON にする。** 対象が使い捨てローカルなので、Active Scan の主リスク
  （不可逆な実害）は「リセットできる」ことで消える。ゆえに初版が持っていた重い安全足場は不要。

### best-effort に緩和してよいもの（＝実験の観測結果として記録するだけ）
- 難しいログインフロー（SPA/CSRF/多段）の認証方式判定に失敗すること
- 単一ユーザーゆえ水平IDOR・水平権限昇格・「別人のトークン」系ができないこと
- Playwright fallback 時に認証付きZAPスキャンが退化すること
- teardown の完璧さ・再認証ループの堅牢さ・網羅テスト・`method: auto` の誤選択

### 実験でも緩めない「床」（安価・3項目のみ）
理由は「安全」ではなく多くが**実験の妥当性**（結果が解釈可能であること）と最小限の衛生：

1. **差分による認証確認**（工程2.5）。これが甘いと「匿名なのに認証成功」と誤合格し、
   **"認証付きDAST成功"と報告されたのに実際は未ログイン=結果がゴミ**になる。これは仕上げでは
   なく**測定器**。最低限「指標が認証時に有り・未認証時に無し」の差分だけは必須。
2. **スキャンごとに forced-user / User 指定の状態を明示**。認証済み走査と未認証走査を取り違えると
   カバレッジ差の観測が濁る。「未ゲートのスキャン前に forced-user を明示OFF、認証付きは User を
   明示指定」の1手。
3. **資格情報を Git・成果物へ平文で残さない／値を印字しない**。リポジトリは公開のため、使い捨て
   アプリのクレデンシャルでも履歴に焼き付けない。`.gitignore`（reports/・.env・ZAPセッション）＋
   値の非印字で足りる。

---

## 1. 基本方針（維持）

- 認証方式ごとの独自認証エンジンを作り込まない。ZAPが持つ Context / Authentication Method /
  Session Management / Verification / User / Forced User Mode / 各認証方式（Browser Based /
  Form / JSON / HTTP Basic / Script / Auto-Detect）を、**LLMが選択して操作**する。
- 利用可能な方式は、使用するZAPバージョンのAPIと公式仕様で確認する（BBA は 2.16.1+）。
- LLMはソース・ログイン画面・DOM・HTTP履歴を見て方式を判断する。特定アプリ専用のログイン処理を
  ハードコードしない。
- **二層構造**：LLM が設定値を判断 → `zap_auth.py` が値を機械的に ZAP API へ反映（判断しない）。

---

## 2. 既存フローへの統合（工程0〜7を維持、工程2.5を挿入）

新Skillに分割せず `/llm-zap-dast:dast` に統合する。フロー構造は変えず、**認証セットアップ＋確認を
工程2.5として1段挿す**だけ。以下は初版からの差分・強化点のみ記す。

### 工程0：安全確認（追加）
- `authentication.enabled` / 認証情報用 env の存在 / 平文ユーザー名・パスワードが書かれていないこと
  / `authentication.active_scan` の値 / ログイン試行上限 / 認証後に到達し得る状態変更機能。
- 認証情報の値は出力しない。

### 工程1：ソース解析（追加）
- ログインURL/API・認証方式候補・資格情報パラメータ名・形式（form/json/basic/token/cookie）・
  セッション/トークン保持方式・**ログイン成功指標の候補**・**ログアウト状態指標の候補**・
  認証後のみ到達可能なURL/API・ログアウトURL・危険URL（パスワード変更/アカウント削除）。
- ソース由来と推測を区別。

### 工程2：診断対象マップ（追加）
- 各エンドポイントに 認証要否 / 必要ロール / 未認証時の期待応答 / 認証後の期待応答 / 状態変更有無 /
  Active Scan 可否 / 認証後に除外すべきか。

### 工程2.5：認証設定と認証確認 ★安全の急所（強化）

工程2と工程3の間。**LLMが `method: auto` を具体方式に解決してから**（＝`zap_auth.py` に `auto` を
渡さない）、Context/認証方式/Session Management/Verification/User を設定し、資格情報を env から
読んで User に設定する。その後、以下の**差分確認を必須**とする：

1. 認証済み User として、**認証後にのみ到達できる URL/API** にテストリクエストを送る。
2. **同じ対象への未認証リクエスト**も取得し、**差分**で判定する：
   - **必須**：認証成功の指標が「**認証時の応答に有り、かつ未認証の応答に無し**」であること。
     - **ステータスのみ／指標の存在のみで合格にしない**（200＋ログイン画面本文、ソフトリダイレクト、
       SPAで指標がJS側にある場合に誤合格するため）。
   - **身元確認**（プローブが身元に依存する場合＝権限昇格/IDOR等）：応答が**意図したユーザー**を
     反映しているか（エコーされたユーザー名/ロール等）を確認する。
3. **確認できた場合だけ**認証後工程へ進む。**認証成功が曖昧・失敗なら「認証済み」として扱わない。**
4. `zap_auth.py test-authentication` は**判定を返さず、生の証拠**（ステータス・本文・ヘッダ・
   指標一致ブール・身元エコー）を返す。**合否判定は LLM ＋ 上記の固定差分ルールが行う。**

失敗しても未認証の診断は継続してよい。ただし**認証後診断は「未実施」として記録**する（run全体は
止めない＝これは capability の欠如であって安全の欠如ではない）。

### 工程3：認証後探索（強化）
- 認証成功時、認証済み User（または認証済みブラウザ）で Traditional Spider / 必要なら Ajax Spider /
  Passive Scan / URL・履歴・アラート取得。
- **認証済みで実行した探索と未認証で実行した探索を明示的に区別**する（成果物 `authentication.md`）。

### 工程4：ブラウザ探索（明確化）
- ブラウザ操作が要る場合は Browser Based Authentication または ZAP 経由 Playwright。Playwright は
  通信を ZAP Proxy へ通す。
- **「Playwright でログインできた」と「ZAP User として認証付き Spider/Active Scan できる」は別**。
  別々に判定・記録する。
- **退化の帰結を明記**：BBA/ZAP-User 認証が使えず **Playwright ログインだけ成功**した場合、
  User指定 Spider（工程3）と User指定 Active Scan（工程5）は **未実施** に落ち、**認証後は工程6の
  LLM 駆動プローブのみ**が認証付きで動く。`authentication.md` とレポートにこの端末状態を書く。

### 工程5：認証付き Active Scan（ON・強化）
- **認証済みか＝継承状態ではなく明示パラメータ**。Active Scan 実行時、認証付きなら User を明示指定、
  **認証付きでないスキャン（既定の未認証 Active Scan 含む）を呼ぶ前に forced-user を明示OFF** する。
  （forced-user は Context 単位のため、放置すると「未認証のつもりの Active Scan」が
  ログイン済みユーザーとして走る＝結果汚染。使い捨て標的でも**ラベリング**のために塞ぐ。）
- **二重ゲート**：`scan.active_scan` かつ `authentication.active_scan` が true、かつ工程5の利用者
  確認を通ったときだけ認証付き Active Scan を実行。利用者へ提示：対象URL/Context/User/認証方式/
  認証後到達URL/除外URL/状態変更URL/Scan Policy/想定負荷/「認証済みゆえ影響範囲が広い」旨。
- 認証後に発見した危険URLを `exclude.paths` 追加候補として提示→確認後に除外。
  **ただし `login_url`/`verification_url` を除外候補に入れない**（認証・再認証が壊れる）。
- Active Scan 中に認証失効の可能性があれば、認証済みスキャン成功と**断定しない**。

### 工程6：LLM による認証後の追加診断（best-effort・維持）
- ソース＋認証後ZAP履歴から仮説→対象固有リクエスト→比較→調整再送→確認/候補/否定/未確認に分類。
- 対象例：認証回避・未認証アクセス・垂直権限昇格・**（単一ユーザーで可能な範囲の）**水平権限昇格/
  IDOR・セッション固定・Cookie属性・JWT/トークン検証不備・ログアウト後セッション無効化・CSRF・
  Mass Assignment・隠しパラメータ・HTTPメソッド変更・認証後 SQLi/XSS/コマンド注入・業務ロジック。
- **単一ユーザーで構造的に不可能な水平系（別人のリソース/トークンが必要なもの）は無理に確定せず、
  「単一ユーザーのため未実施」と記録**する。実施できた範囲まで試し、できなかった理由を残す。
- **ログアウトを誘発するプローブ（セッション無効化・ログアウト後・別トークン）の実行中は
  自動再認証（forced-user）をOFF**にし、終了後に意図的に張り直す。これをしないと再ログイン連打で
  ロックアウト＋`max_attempts` 枯渇→**以降が静かに匿名化**する。

### 工程7：レポート（追加）
- 認証方式 / 認証設定結果 / 認証確認結果 / 認証後探索結果 / 認証付き Active Scan 結果 /
  認証状態を維持できたか / 未認証と認証後のカバレッジ差 / LLM追加診断結果 / 実施できなかった認証工程 /
  未確認範囲。状態は 成功 / 失敗 / 未確定 / 未実施 で明示。

---

## 3. 状態モデル（維持）

工程ごとに記録：認証設定・認証確認・認証後Spider・認証後Passive・認証付きActive Scan・LLM追加診断＝
各 成功 / 失敗 / 未確定 / 未実施 ＋ 理由 ＋ 診断できなかった範囲。
**認証成功が曖昧なら認証済みとして扱わない。**
**`max_attempts` 枯渇・認証失効は「認証失敗（decayed）→認証部分を停止」**として分類する
（＝静かに匿名継続しない）。未認証診断の継続は妨げない。

---

## 4. 設定スキーマ

`dast.yaml` の `authentication` を拡張：

```yaml
authentication:
  enabled: false

  # auto | browser | form | json | basic | script
  method: auto

  login_url: /login

  username_env: DAST_USERNAME
  password_env: DAST_PASSWORD

  max_attempts: 3

  verification:
    method: auto            # auto | url | indicator
    logged_in_indicator:    # 任意。LLMが導出してもよい
    logged_out_indicator:
    verification_url:

  session_management:
    method: auto            # auto | cookie | header | script

  # 認証付き Active Scan の追加ゲート。scan.active_scan とAND。
  active_scan: false        # 新規設定では既定OFF（実験で使うなら明示的に true）
```

要件：
- 既定 `enabled: false`。既存の未認証設定との後方互換を維持。
- **平文の `username` / `password` キーは禁止**（構造的に拒否）。資格情報は env からのみ。
- `method: auto` は LLM が対象解析で具体方式へ解決（スクリプトに `auto` を渡さない）。
- 未指定項目は推測してよいが採用値と理由を記録。推測できなければ認証工程を失敗として終了
  （＝認証段のみ終了。runは未認証で継続）。
- `scan.active_scan` は全体ゲート、`authentication.active_scan` は認証付き Active Scan の追加ゲート。
  **両方 true ＋ 工程5確認** で実行。
- **`login_url` / `verification.verification_url` が `exclude.paths` に該当したらエラー**。

`validate_config.py` に上記を実装（後述テスト）。

---

## 5. スクリプト `scripts/zap_auth.py`（薄いラッパ・判断しない）

対象固有の認証手順をハードコードしない。LLM がソース・画面を解析して**どの引数を渡すか**を判断し、
スクリプトは**引数どおり ZAP API へ反映するだけ**。想定コマンド：

```text
detect-capabilities        # 利用可能な認証/セッション方式を取得（未対応方式を明示）
create-context
configure-authentication   # method は具体値のみ（auto 不可）
configure-session-management
configure-verification
create-user
set-credentials            # env から読む。値は返さない/印字しない
test-authentication        # 生の証拠（status/body/headers/指標一致/身元エコー）を返す。合否は返さない
set-forced-user            # on/off を明示。未ゲートスキャン前に off にできる
spider-as-user
ajax-spider-as-user
active-scan-as-user        # gate-passed フラグ必須。未通過なら実行しない
auth-status
clear-authentication       # teardown。User/Context/資格情報を消す
```

- **`test-authentication` は判定を返さない**（生の証拠のみ）。合否は LLM＋固定差分ルール（工程2.5）。
- **`configure-authentication` は `method: auto` を受け取らない**（受けたらエラー）。
- **`active-scan-as-user` は gate-passed を必須引数**とし、未通過なら呼び出しを拒否。
- `detect-capabilities` で ZAP バージョン差を吸収し、未対応方式を明示。

---

## 6. redaction（漏れ口を塞ぐ）

必ずマスク：ユーザー名 / パスワード / Cookie / Set-Cookie / Authorization / Basic認証値 / JWT /
Access/Refresh Token / CSRF Token / ログインPOST本文。

資格情報の**値**を次へ出さない：run.log / JSON出力 / stdout / stderr / report.md /
authentication.md / screenshots / Playwright trace。

- `redact.py` を拡張：設定のログインフィールド名（`username_env`/`password_env` の実フィールド名）を
  動的に渡してマスク。`*password*` / `*passwd*` の部分一致。**ユーザー名の値もマスク**。
- **ZAP User に保存された資格情報は `redact.py` では消せない**（バイナリセッションに届かない）。
  対策は**マスクではなく削除**：`clear-authentication` を run 終了時と中断時に**必ず**呼び、
  一時 User/Context/ブラウザプロファイル/一時ファイルを消す。**ZAPセッションをリポジトリ配下に
  置かない。** 消せなかった場合は成果物に警告。

---

## 7. 安全性（維持する既存ルール）

allowed_hosts 外へ送らない / ATTACKモード不使用 / 本番既定拒否 / Active Scan前に明示確認 /
exclude.paths 適用 / DoS相当なし / 不可逆操作を自動実行しない / 認証情報を成果物へ保存しない /
ログイン試行は `max_attempts` 以下 / 認証成功が不明なら認証済みと扱わない。
認証後 Active Scan は未認証より影響範囲が広いことを確認画面とレポートに明記。

---

## 8. テスト（安全の床を assert する）

実物アプリ不要の単体テスト中心（既存方針）。少なくとも：
- `authentication.enabled: false` の後方互換
- 平文資格情報キー（`username:`/`password:`）を拒否
- 環境変数名不足を検出
- `max_attempts` の検証
- `login_url` / `verification_url` が `exclude.paths` に該当したらエラー
- `authentication.active_scan` false なら認証付き Active Scan を呼ばない
- `zap_auth.py`：`configure-authentication` が `auto` を拒否、`active-scan-as-user` が
  gate-passed なしを拒否、`test-authentication` が合否ではなく証拠を返す
- **redaction：ユーザー名の値・`user_password=` 等の非標準フィールドが `reports/<run-id>/` ツリー
  全体（run.log 含む）に残らない**（成果物ツリー全体を grep）
- 差分確認の考え方：認証時・未認証時の両方に出る指標は「確認失敗」（このロジックを持つ関数を用意し
  単体テスト）
- 認証失敗がレポートに記録される / 認証後の新規URLを区別できる

---

## 9. 実装順（段階・途中成果を残せる順）

1. 設定スキーマ ＋ `validate_config.py` ＋ 検証テスト（後方互換・平文拒否・exclude衝突・二重ゲート）
2. `redact.py` 拡張 ＋ テスト（ユーザー名・非標準フィールド・ツリー全体）
3. `zap_auth.py`（薄いラッパ）＋ テスト（auto拒否・gate必須・test-authは証拠のみ）
4. SKILL.md（工程2.5挿入・工程3/5/6・cleanup）＋ references 更新
5. テンプレート（`authentication.md`）・README（認証を「未実装」から外し best-effort と明記）

各段で途中まででも成果物を残せる。ZAP の認証 API に制約が見つかっても、その時点までを残す。

---

## 10. 変更対象ファイル

`README.md` / `SKILL.md` / `references/zap-integration.md` / `references/safety-policy.md` /
`references/source-analysis.md` / `references/scenario-testing.md` / `references/redaction.md` /
`references/report-format.md` / `references/config-init.md` / `templates/dast-config.example.yaml` /
`templates/report.example.md` / 新規 `templates/authentication.example.md` / `examples/dast.yaml` /
`scripts/validate_config.py` / 新規 `scripts/zap_auth.py` / `scripts/redact.py` / 関連テスト。

README には**この認証対応が best-effort であり、任意のWebアプリでの認証成功を保証しないこと**、
**対象は使い捨てローカルを前提とすること**を明記。
